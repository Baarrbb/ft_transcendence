apiVersion: 1

providers:
  - name: Default
    orgId: 1
    folder: ''
    folderUid: ''
    type: file
    updateIntervalSeconds: 15
    allowUiUpdates: ${EDIT_DASHBOARDS}
    options:
      path: /var/lib/grafana/dashboards
